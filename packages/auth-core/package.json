{
  "name": "@me-and/auth-core",
  "version": "3.0.0",
  "description": "Session validation, shared across Me & products. Validates Supabase-issued access tokens via the Auth API rather than local signature verification, so it works with both symmetric and asymmetric token signing.",
  "main": "src/index.js",
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
